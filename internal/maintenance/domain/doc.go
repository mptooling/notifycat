// Package domain holds the maintenance domain's contracts: the ports, DTOs,
// enums, and constants for stale-message cleanup and PR reconcile. It depends
// only on the standard library and the shared kernel.
package domain
