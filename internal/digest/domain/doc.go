// Package domain defines the digest domain's ports, DTOs, and constants — the
// scheduled stuck-PR reminder contracts. It imports only the standard library,
// the shared kernel, and the routing domain.
package domain
