// Package application holds the notification use cases — the PR-lifecycle
// handlers (open/close/draft and the review-reaction handlers) and the event
// dispatcher — behind the domain's Handler and use-case interfaces.
package application
