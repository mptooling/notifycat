// Package domain defines the notification domain's ports, DTOs, enums, and
// policies — the contracts for keeping one chat message per (PR, channel) in
// sync with GitHub PR events. It imports only the standard library, the shared
// kernel, and the routing domain.
package domain
