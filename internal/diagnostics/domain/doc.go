// Package domain defines the diagnostics domain's ports, DTOs, and constants —
// the operator-tooling contracts (preflight doctor, config CLI, smoke delivery).
// It imports only the standard library, the shared kernel, and the validation
// and routing domains.
package domain
