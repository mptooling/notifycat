// Package domain defines the review domain's ports, DTOs, and errors — the
// interactive "Start review" flow's contracts. It imports only the standard
// library and the shared kernel.
package domain
